---
# - name: Bootstrap OpenWRT to enable Ansible
#   hosts:
#     - openwrt_office
#   become: true
#   gather_facts: false
#   roles:
#     - servers/openwrt/bootstrap-ansible

- name: Setup DHCP on OpenWRT
  # pre_tasks:
  #   - ansible.builtin.include_vars: ../
  hosts:
    - openwrt_office
  become: true
  gather_facts: true
  roles:
    # - servers/openwrt/bootstrap
    - servers/openwrt/dhcp
