---
- name: Create cloud-init config
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ libvirt_cloud_init_src_dir }}/{{ vm.name }}/{{ item.dest }}"
    mode: "0644"
  loop: "{{ libvirt_cloud_init_config_files }}"

- name: Create CD-ROM ISO with cloud-init config
  ansible.builtin.shell: |
    genisoimage -output {{ libvirt_vm_cloud_init_path }} \
      -volid cidata \
      -joliet \
      -r {% for item in libvirt_cloud_init_config_files %} {{ libvirt_cloud_init_src_dir }}/{{ vm.name }}/{{ item.dest }} {% endfor %} &>> "{{ libvirt_cloud_init_src_dir }}/{{ vm.name }}/logs"
