---
- name: Ensure install dir exists
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ home_assistant_install_path }}"

- name: Configure docker-compose file
  ansible.builtin.template:
    src: "{{ item }}"
    dest: "{{ home_assistant_install_path }}/{{ item }}"
    mode: "0664"
    backup: true
  loop:
    - docker-compose.yaml

- name: Pull Docker image using docker-compose
  ansible.builtin.command: docker-compose pull
  changed_when: home_assistant_pull_result.stderr_lines | length > 2
  register: home_assistant_pull_result
  args:
    chdir: "{{ home_assistant_install_path }}"

- name: Run using docker-compose
  ansible.builtin.command: docker-compose up -d
  changed_when: home_assistant_up_result.stderr_lines | length > 2
  register: home_assistant_up_result
  args:
    chdir: "{{ home_assistant_install_path }}"

# - name: Restart using docker-compose
#   ansible.builtin.command: docker-compose restart
#   args:
#     chdir: "{{ home_assistant_install_path }}"

# https://community.home-assistant.io/t/configuration-not-honored-during-initial-onboarding-process/196178/13
# hass --script auth --config /config change_password
