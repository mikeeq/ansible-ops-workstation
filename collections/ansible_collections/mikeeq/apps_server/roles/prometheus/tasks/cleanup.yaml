---
- name: Stop container using docker-compose
  ansible.builtin.command: docker-compose stop
  changed_when: prometheus_up_result.stderr_lines | length > 1
  register: prometheus_up_result
  args:
    chdir: "{{ prometheus_install_path }}"

- name: Remove all volumes/containers using docker-compose
  ansible.builtin.command: docker-compose rm -vf
  args:
    chdir: "{{ prometheus_install_path }}"

- name: Ensure prometheus_install_path dir is removed
  ansible.builtin.file:
    path: "{{ prometheus_install_path }}"
    state: absent
