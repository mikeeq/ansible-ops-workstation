---
version: 2.1
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: cimg/base:2024.07
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Run tests
          command: |
            echo tests
