# Based on: https://github.com/DBarthe/ansible-libvirt-cloudinit
---
- import_tasks: setup.yaml

- include_tasks: create_vm.yaml
  loop: "{{ vms }}"
  loop_control:
    loop_var: vm

- include_tasks: destroy_vm.yaml
  loop: "{{ vms_to_remove }}"
  loop_control:
    loop_var: vm
