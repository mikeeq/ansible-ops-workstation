---
- name: disable firewalld
  systemd:
    name: firewalld
    enabled: false
  tags:
    - dont_run_in_docker

- name: install iptables
  dnf:
    name: "{{ pkgs }}"
    state: present
  vars:
    pkgs:
      - iptables
      - iptables-services
