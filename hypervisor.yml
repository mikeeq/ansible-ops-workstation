---
- name: Fedora host
  hosts: "{{ hosts_to_deploy | default('local') }}"
  become: true
  pre_tasks:
    # - debug:
    #     var: hostvars[inventory_hostname]
  roles:
    # - desktop/machines/macbook_pro_2020
    - servers/network-baremetal
  #   - servers/libvirt
